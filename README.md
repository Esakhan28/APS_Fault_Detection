First of create an env file and create a mongodb cluster in it and keep the cluster link in a variable MONGO_DB_URL = "" (The dataset is already uploaded as aps_failure_detection)
Fast-API is used , feel free to replace with django or flask
There are several steps and running may take time
Only run main.py file
